# 开发说明

## 当前版本：VPP-1

VPP-1 只做上游任务生成与 JSON 查询命令发布。它用于验证 VPP 接收到城市级/区域级任务后，能否生成结构清晰、可交给建筑侧 Agent / EnergyPlus 的能力查询命令。

## 后续版本路线

- VPP-2：接收建筑侧 `FlexibilitySummary` 并生成控制/邀请策略。
- VPP-3：接入 EnergyPlus 真实仿真反馈。
- VPP-4：加入结算与可靠性更新。

## 开发原则

VPP-1 不保留旧接口，不做兼容层。任何新增功能应围绕当前最小链路展开，避免提前引入完整 VPP 平台复杂度。
