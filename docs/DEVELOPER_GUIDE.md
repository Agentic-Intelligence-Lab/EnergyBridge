## Environment Setup

If Miniconda is installed under `~/miniconda3`, activate the environment with:

```bash
cd ~/work/EnergyBridge
source ~/miniconda3/etc/profile.d/conda.sh
conda activate energybridge